# משנתי — מסמך למתכנת (DEV) · **קרא אותי קודם**

מסמך ההתמצאות הראשי. קרא אותו לפני שאתה נוגע בקוד. ה-`CHANGELOG.md` הוא יומן כרונולוגי משלים.

**פרויקט:** משנתי (שם עבודה קודם: "משניון"). מזלג עצמאי של "תהילהון" שהוסב מלימוד תהילים ללימוד משניות.
**Stack:** HTML/CSS/JavaScript טהור (ללא framework) · Firebase (Auth אנונימי + Firestore) · PWA (Service Worker) · פריסה: Vercel (מ-`main`) · טקסטים: Sefaria API · קול גברי איכותי: Google Cloud TTS (אופציונלי, דרך פונקציית Vercel).
**ריפו:** github.com/nehemia99-netizen/Mishnayot · ענף `main` · פרוס ב-`mishnayot-alpha.vercel.app`.
**גרסה נוכחית:** 1.5.69 · **אפליקציית אחות:** תהילהון (ריפו Tehilon, פרויקט Firebase `tehilhon`, `tehilon.vercel.app`) — מנוע כמעט זהה; ראה הבדלים בסוף.

---

## 1. מפת קבצים

| קובץ | תפקיד |
|---|---|
| `corpus.js` | **עמוד השדרה.** מבנה המשנה (6 סדרים · 63 מסכתות · 525 פרקים · 4192 משניות). חושף `window.Corpus` (`parseChapId`, `chapId`, `apiRef`, `getTractate`...). נוצר אוטומטית מ-Sefaria. **אין לערוך ידנית.** |
| `01-dashboard.html` | עמוד הבית: לימוד לשמה/מסלול אתגרי, גרף, טיקר, חיפוש, באנר "המשנה שלי", כרטיס SRS, קהילה. |
| `02-selection.html` | בחירה היררכית (סדר→מסכת→פרק) + לשונית מועדפים (כוכב "המשנה שלי" + כפתור הסרה). |
| `03-reader.html` | **הקורא — הלב.** טקסט+פירוש, ניווט, 7 מצבי שינון (שבב+גיליון), הקלטה עצמית, הקראה (TTS מכשיר + ענן), סימנים, מבחן+מאסטרי. |
| `04-history.html` | היסטוריה/הישגים: מפת 525 פרקים, קוביות רצף 28 ימים (סופ"ש בתכלת), מדליות/תעודות, אתגרים. |
| `05-yizkor.html` | לעילוי נשמה/רפואה + לימוד חופשי עם הקדשה. |
| `06-settings.html` | הגדרות: גופן, שפה, תזכורות, **השתקת שבת**, קול הקראה (+כוונון קול ענן), הסרת "המשנה שלי", גיבוי. |
| `07-community.html` / `08-group.html` | קהילה/קבוצות (Firestore, אימות אנונימי). בקהילה: **5 לוחות מובילים מרובי-מדדים** (טאבים → `renderLeaderboard/switchLb`, `LB_METRICS`). |
| `09-saved.html` / `10-sequence.html` | שמורים / מחולל רצף. |
| `blessings.html` | ברכות + לימוד יומי: פרקי אבות, רמב"ם יומי, **חפץ חיים יומי**. |
| `gamification.js` | זכויות, רצפים, רמות, אתגרים, badges, **SRS** (Leitner). חושף `window.Gamification`. |
| `i18n.js` | מילון he/en. `applyTranslations()` מציב **innerHTML** לכל `[data-i18n]`. |
| `notifications.js` | תזכורות מקומיות + גידור שבת. |
| `fcm.js` / `firebase-messaging-sw.js` | Push (FCM) — לקוח + SW רקע. |
| `shake.js` | שייק→פרק אקראי (DeviceMotion). נטען **בכל הדפים**. |
| `errlog.js` | לוגינג שגיאות גלובלי → Firestore `errorLogs` (`create` בלבד). נטען בכל הדפים. |
| `sw.js` | Service Worker — Network-First לאפליקציה ול-Sefaria, Cache-First ל-CDN. |
| `api/tts.js` | **פונקציית Vercel** — פרוקסי ל-Google Cloud TTS (קול גברי). מפתח ב-env `GOOGLE_TTS_KEY`. |
| `scripts/` | סקריפטי GitHub Actions: `send-daily-push.js` (פוש יומי), `notify-new-members.js`, `survey-report.js` (דוח סקר מעובד → GITHUB_STEP_SUMMARY). סוד: `FIREBASE_SA`. |
| `vercel.json` | `Cache-Control: no-cache` ל-`/sw.js` ולכל `*.html`. |

---

## 2. מודל הנתונים — קריטי

**`chap` הוא מזהה-מחרוזת** `"Berakhot.2"` (מסכת.פרק), **לא מספר!** לעולם אל תעשה `parseInt(chap)`/`+chap`/`toGematria(chap)` — השתמש ב-`Corpus.parseChapId(id)` או `String(chap)`. (בתהילהון `chap` הוא **מספר** — שים לב בהעברת קוד.)

מפתחות ה-localStorage שומרים בכוונה את הקידומת הישנה `tehillim_*` כדי לא לשבור נתוני משתמשים:
- `tehillim_readings` · `tehillim_favs` · `tehillim_mem_m` (משניות בע"פ `"chapId:m"`) · `tehillim_my_chapter` · `tehillim_dedication` · `tehillim_playlist` · `tehillim_settings` · `tehillim_gamification`.
- `mishnah_mastery` · `mishnah_srs` (Leitner) · `mishnah_notes`.
- **IndexedDB:** `mishnayon_rec` (הקלטות, store `rec`, מפתח `"chapId:m"`) · `mishnayon_tts` (קאש אודיו ענן, store `audio`) · `mishnayon_prefs` (store `kv`, מפתח `noShabbatNotif` — נקרא ע"י ה-SW ברקע).
- **שים לב:** עוטף ה-localStorage `S = {get,set}` מוגדר **מקומית בכל דף** — *לא* ב-utils.js. **ל-`S` בדשבורד אין `remove`** (השתמש ב-`S.set(k,null)` או `localStorage.removeItem`). בעמוד הבחירה `S` *כן* כולל `remove`.

---

## 3. 🏆 מלכודות וכללי זהב — **החלק הכי חשוב** (כדי לא לחזור על טעויות)

**קוד / מוסכמות:**
1. **`chap` מחרוזת מול מספר.** קוד מתקופת תהילים עשה `parseInt(d.id)` → NaN ("פרק NaN"/"undefined"). תבנית חוזרת — חפש אותה תמיד.
2. **`S` משתנה בין דפים.** דשבורד: `{get,set}` בלבד (אין `remove`!). בחירה: `{get,set,remove}`. אל תניח שיש `remove`.
3. **`showToast` לא תמיד מוגדר.** בעמוד הבחירה של תהילהון הוא נקרא אך לא הוגדר → ענף ההסרה זרק שגיאה והכוכב לא התעדכן. ודא שהפונקציה קיימת לפני שקוראים לה, או הגדר fallback. **כלל: עדכן תצוגה *לפני* קריאת toast.**
4. **טקסט גלוי רבים מ-i18n.js (DICT) ומוצב כ-innerHTML.** ערוך שם, ובמפ את `?v=` של i18n.js.
5. **תבנית `<title>` חסר תג-פתיחה** — דלף טקסט גלוי ודחף את העמוד שמאלה. להיזהר ב-copy-paste.

**Sefaria / טקסטים:**
6. **ה-ref של "כלל N" בחפץ חיים מתפצל אוטומטית לסעיף א בלבד** (`heRef: "...כלל ב א׳"`). זה היה שורש באג "התקוע" — כל ימי הכלל הראו אותו משפט. הפתרון (`_ccFetchPieces` ב-blessings): לאסוף את כל הסעיפים `N:1, N:2, ...` עד תוצאה ריקה, ולהציג סעיף אחר בכל יום. **לקח: ל-ref ברמת-section ב-Sefaria בדוק אם הוא מחזיר את כל הילדים או רק את הראשון.**
7. **לימודים יומיים נקיים** (אבות/רמב"ם) משתמשים בדפוס **פריט-אחד-ליום מרשימה שטוחה** (`idx % length`) — אין שם באג. רק חפץ חיים השתמש בחלוקת תת-קטעים → ושם היה הבאג.

**הקראה / אודיו:**
8. **Web Speech API לא ניתן לעיבוד-אודיו בדפדפן** (הקול לא נתב ל-Web Audio) — אי אפשר "להפוך" את הקול לבס גברי. הקול ה"גברי" המובנה = הורדת pitch ל-0.5 (נשמע מלאכותי). לקול גבר אמיתי → **TTS ענן** (`api/tts.js`, Google `he-IL-Wavenet-D`, pitch שלילי = עמוק וטבעי).
9. **Autoplay בנייד:** נגן אודיו של הענן חייב "שחרור" בתוך מחוות-המשתמש (`_ttsUnlockAudio` משמיע WAV שקט ב-ttsStart). אחרת ה-`.play()` הראשון (אחרי fetch אסינכרוני) נחסם.
10. **Wake Lock** משוחרר כשהטאב יורד לרקע — לבקש מחדש ב-`visibilitychange`.
11. **קול עברי ברירת מחדל נשי** (Carmit) ברוב המכשירים. אין שליטה אמיתית על מין הקול בלי קול מתאים מותקן.

**תשתית / פריסה:**
12. **Service Worker מגיש קבצים ישנים** — תוקן ע"י reload ב-`controllerchange` + `vercel.json` no-cache. בכל פריסה: **במפ `CACHE_VER`** (sw.js) + מחרוזת גרסה בדשבורד והגדרות + **`?v=`** של כל JS משותף ששונה (i18n/notifications/fcm/shake/gamification). אחרת הדפדפן מגיש ישן.
13. **גרסאות:** מקדמים רק את הספרה השלישית, דו-ספרתי (1.5.xx).
14. **פריסת Vercel נתקעת לפעמים** (commit נדחף אך הפריסה לא עולה). פתרון: `git commit --allow-empty` כדי להפעיל פריסה נקייה. לאמת חי: `curl .../sw.js | grep CACHE_VER`.
15. **קידוד עברי מעורב פר-קובץ** (BOM+CRLF/LF). למחיקה/החלפה רבת-קבצים — **החלפה ברמת-בייטים ב-Python** (משמרת BOM/EOL במדויק, לא נוגעת במזהי אחסון). לא `sed`.
16. **Firestore דורש אימות אנונימי לקריאות.** ב-localhost האימות נכשל → קהילה/מובילים יראו "אין נתונים" בתצוגה מקדימה — תקין.
16א. **`hasOnly` + `merge:true` ב-leaderboard:** `setDoc(...,{merge:true})` שולח ל-`request.resource.data` את **כל** מפתחות המסמך לאחר המיזוג — לכן ה-`hasOnly` בחוק חייב לכלול את *כל* השדות שהמסמך אי-פעם יכיל, לא רק את אלה שהשתנו. הוספת שדה חדש ללוח מובילים = חובה להוסיף אותו גם ל-`hasOnly` **וגם** לפרסם בקונסולה, אחרת כל הכתיבה נחסמת.
16ב. **שדות חדשים בלוח מובילים מתמלאים בהדרגה:** מסמכים קיימים חסרים את השדה החדש, ולכן `orderBy(field,'desc')` מחזיר רק את מי שכבר סנכרן (מסמכים בלי השדה מוחרגים). לוח חדש יציג empty-state עד שמשתמשים יכנסו לקורא ויסנכרנו — תקין, לא באג.
17. **משתני סביבה ב-Vercel:** שדה **Key** = שם המשתנה (`GOOGLE_TTS_KEY`), שדה **Value** = הסוד. אחרי הוספה — **Redeploy** (חל רק על פריסה חדשה). Google TTS דורש **Billing מופעל** על הפרויקט (גם למכסה החינמית).

**Push / שבת:**
18. **`send-daily-push.js` שולח לכל ה-pushTokens** — לא קשור למתג "תזכורת יומית" המקומי. מספיק שמתג ה-Push דלוק כדי לקבל. cron של GitHub Actions מתעכב (לכן הגיע 23:47). השרת חוסם **שעות שקטות 22:00–08:00** ו**שבת** (שעון ישראל) כברירת מחדל.
19. **שייק:** באנדרואיד להתחיל להאזין **מיד** (לא להמתין ל-gesture — זה התקלקל); iOS דורש gesture להרשאת חיישן.

---

## 4. מוכנות להשקה לציבור — צ'קליסט עם סטטוס (נכון ל-1.5.69)

| פריט | סטטוס | הערה |
|---|---|---|
| שם סופי "משנתי" | ✅ הושלם | בכל הטקסט הגלוי, manifest, שיתופים, תעודות, i18n. |
| **הסרת "BETA"** | ❌ **נותר** | עדיין "גרסה 1.5.xx **BETA**" בדשבורד ובהגדרות. |
| **לוגו אמיתי** | ❌ **נותר** | עדיין `4.jpg` פלייסהולדר (4 מופעים). |
| הקשחת חוקי Firestore | ⚠️ פעולה ידנית | `firestore.rules` בריפו מעודכן (כולל `errorLogs`, `surveys`, ו-whitelist `leaderboard` מורחב). **נפרס ידנית בקונסולה — לא דרך Vercel.** בכל שינוי חוק: לפרסם בקונסולה אחרת הכתיבה תיכשל בשקט. |
| QA במכשירים אמיתיים | ⚠️ חלקי | נבדק ע"י המשתמש (TTS, שינון, חפץ חיים, גרף, לוחות מובילים). **חסר:** בדיקת מיקרופון/הקלטה, התקנת PWA, אופליין, עדכון SW במכשיר אמיתי (iOS+Android). |
| לוגינג שגיאות | ✅ קיים | `errlog.js` נוסף ב-1.5.63 (היה חסר). |
| סקר משתמשים מובנה | ✅ קיים | הגדרות → Firestore `surveys` → `survey-report.js` (דוח מעובד ב-Action). |
| תקנון/פרטיות | ✅ קיים | `terms.html`. כדאי קריאה אחרונה. |
| לוח חפץ חיים רשמי | ⚠️ קירוב | מציג סעיף-ליום אמיתי, אך החלוקה לימים היא קירוב (לא הלוח הרשמי). הוחלט לקבל. |
| מטמון/סקלביליות לקורפוס | ⬜ לא נדרש דחוף | מטמון קבוע ל-525 פרקים לאופליין מלא — שיפור עתידי. |
| הגרלת "נחש את הספר" | ⬜ החלטה פתוחה | להמיר ל-6 סדרים או להסיר. |

**מסקנה: 2 חוסמים אמיתיים לפני השקה — הסרת BETA + לוגו אמיתי.** השאר רצוי/שיפור. תזכורת קבועה: בכל פריסה שנוגעת ב-`firestore.rules` — **לפרסם ידנית בקונסולת Firebase**.

---

## 5. רשימת מגירה / עתיד
- SRS פר-משנה (לא רק פר-פרק) + סשן חזרה משורשר.
- העברת קול-ענן/הקלטה/שינון-גיליון לתהילהון בוצעה ✅ (חוץ מקול-ענן — לא הועבר בגלל אורך פרקים/עלות).
- לוח חפץ חיים רשמי לפי תאריך עברי (טבלת מיפוי ידנית).
- מטמון קבוע לטקסטים שנלמדו; ליטוש מצב כהה; שיתוף/הדפסת משנה.

---

## 6. הבדלים מתהילהון (להעברת קוד)
- **`chap` מספרי** בתהילהון מול **מחרוזת** במשנתי — התאמה בכל קוד מועבר.
- IndexedDB: `tehillon_rec` / `tehillon_prefs` (לא `mishnayon_*`).
- פרויקט Firebase `tehilhon` · ריפו Tehilon · `tehilon.vercel.app`.
- בתהילהון **יש `errlog.js`**, **אין קול-ענן** (`api/tts.js`). פרקי תהילים ארוכים → עלות TTS גבוהה יותר.
- ב-`S` של עמוד הבחירה בתהילהון אין `showToast` מוגדר (ראה מלכודת #3).
