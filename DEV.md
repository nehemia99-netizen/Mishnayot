# משנתי — מסמך למתכנת (DEV) · **קרא אותי קודם**

מסמך ההתמצאות הראשי. קרא אותו לפני שאתה נוגע בקוד. ה-`CHANGELOG.md` הוא יומן כרונולוגי משלים.

**פרויקט:** משנתי (שם עבודה קודם: "משניון"). מזלג עצמאי של "תהילהון" שהוסב מלימוד תהילים ללימוד משניות.
**Stack:** HTML/CSS/JavaScript טהור (ללא framework) · Firebase (Auth אנונימי + Firestore) · PWA (Service Worker) · פריסה: Vercel (מ-`main`) · טקסטים: Sefaria API + **חבילת טקסט מקומית לאופליין** · קול גברי איכותי: Google Cloud TTS (אופציונלי, דרך פונקציית Vercel).
**ריפו:** github.com/nehemia99-netizen/Mishnayot · ענף `main` · פרוס ב-`mishnayot-alpha.vercel.app`.
**גרסה נוכחית:** 1.5.81 (BETA) · **אפליקציית אחות:** תהילהון (ריפו Tehilon, פרויקט Firebase `tehilhon`, `tehilon.vercel.app`) — מנוע כמעט זהה; ראה הבדלים בסוף.
**משפחת האפליקציות:** משנתי · תהילהון · **עמוד הזמן** (`jewish-timeline.vercel.app`, React/Vite — ריפו נפרד). כל אחת מקשרת לשתיים האחרות בהגדרות ("האפליקציות שלנו").

---

## 1. מפת קבצים

| קובץ | תפקיד |
|---|---|
| `corpus.js` | **עמוד השדרה.** מבנה המשנה (6 סדרים · 63 מסכתות · 525 פרקים · 4192 משניות). חושף `window.Corpus` (`parseChapId`, `chapId`, `apiRef`, `getTractate`...). נוצר אוטומטית מ-Sefaria. **אין לערוך ידנית.** |
| `mishnah-text/` | **חבילת אופליין: 63 קבצי טקסט פר-מסכת** (~3MB סה"כ). כל קובץ מציב `window.MISHNAH_TEXT[<tractateId>] = { <perek>: {he:[...], heRef} }`. נטענים **על-פי דרישה** בקורא; נשמרים ב-SW בהתקנה. ראה מלכודת #20. |
| `01-dashboard.html` | עמוד הבית: לימוד לשמה/מסלול אתגרי, גרף, טיקר "בורסת המשנה", חיפוש, באנר "המשנה שלי", כרטיס SRS, קהילה, **באנר התקנת PWA**. |
| `02-selection.html` | בחירה היררכית (סדר→מסכת→פרק) + לשונית מועדפים (כוכב "המשנה שלי" + כפתור הסרה). |
| `03-reader.html` | **הקורא — הלב.** טקסט+פירוש, ניווט, 7 מצבי שינון (שבב+גיליון), הקלטה עצמית, הקראה (TTS מכשיר + ענן), סימנים, מבחן+מאסטרי, **מונה חי צף "RayBan"**. |
| `04-history.html` | היסטוריה/הישגים: מפת 525 פרקים, קוביות רצף 28 ימים (סופ"ש בתכלת), מדליות/תעודות, אתגרים, פרקים הכי נצפים, מס' 1 בלוח. |
| `05-yizkor.html` | לעילוי נשמה/רפואה + לימוד חופשי עם הקדשה. |
| `06-settings.html` | הגדרות: גופן, שפה, תזכורות, **השתקת שבת**, קול הקראה (+כוונון קול ענן), הסרת "המשנה שלי", גיבוי, **כפתור התקנה**, **"האפליקציות שלנו"**. |
| `07-community.html` / `08-group.html` | קהילה/קבוצות (Firestore, אימות אנונימי). בקהילה: **5 לוחות מובילים מרובי-מדדים** (`LB_METRICS`, `renderLeaderboard/switchLb`). |
| `09-saved.html` / `10-sequence.html` | שמורים / מחולל רצף. |
| `blessings.html` | ברכות + לימוד יומי: פרקי אבות, רמב"ם יומי, **חפץ חיים יומי**. |
| `auth.js` | **Google Login + אנונימי + סנכרון Firestore** (גיבוי/שחזור ענן חוצה-מכשירים). |
| `gamification.js` | זכויות, רצפים, רמות, אתגרים, badges, **SRS** (Leitner). חושף `window.Gamification`. |
| `i18n.js` | מילון he/en. `applyTranslations()` מציב **innerHTML** לכל `[data-i18n]`. |
| `notifications.js` · `fcm.js` · `firebase-messaging-sw.js` | תזכורות מקומיות + גידור שבת · Push (FCM) לקוח + SW רקע. |
| `shake.js` · `errlog.js` | שייק→פרק אקראי (בכל הדפים) · לוגינג שגיאות → Firestore `errorLogs` (`create` בלבד). |
| `sw.js` | Service Worker — Network-First לאפליקציה ול-Sefaria, Cache-First ל-CDN. מכיל `APP_SHELL` + **`MISHNAH_TEXT_FILES`** (63 קבצים, precache best-effort). |
| `api/tts.js` | **פונקציית Vercel** — פרוקסי ל-Google Cloud TTS (קול גברי). מפתח ב-env `GOOGLE_TTS_KEY`. |
| `scripts/` | GitHub Actions: `send-daily-push.js`, `notify-new-members.js`, `survey-report.js`. סוד: `FIREBASE_SA`. |
| `vercel.json` | `Cache-Control: no-cache` ל-`/sw.js` ולכל `*.html`. |
| `STORE-LISTING.md` · `DATA-SAFETY.md` | חומרי Google Play מוכנים (he+en) · תשובות טופס Data Safety. |

> **חומרי השקה מחוץ לריפו:** דף נחיתה דו-לשוני + קודי QR ב-`...\אפליקציה תהילים\landing-mishnati\` (כולל `qr/`). מקומי, לא git-tracked — לפרוס ל-Vercel בנפרד.

---

## 2. מודל הנתונים — קריטי

**`chap` הוא מזהה-מחרוזת** `"Berakhot.2"` (מסכת.פרק), **לא מספר!** לעולם אל תעשה `parseInt(chap)`/`+chap`/`toGematria(chap)` — השתמש ב-`Corpus.parseChapId(id)` או `String(chap)`. (בתהילהון `chap` הוא **מספר** — שים לב בהעברת קוד.)

מפתחות ה-localStorage שומרים בכוונה את הקידומת הישנה `tehillim_*` כדי לא לשבור נתוני משתמשים:
- `tehillim_readings` · `tehillim_favs` · `tehillim_mem_m` (משניות בע"פ `"chapId:m"`) · `tehillim_my_chapter` · `tehillim_dedication` · `tehillim_playlist` · `tehillim_settings` · `tehillim_gamification` (byDay = מקור-אמת לזכויות).
- `mishnah_mastery` · `mishnah_srs` (Leitner) · `mishnah_notes` · `mishnah_cache_<chapId>` (טקסט פר-פרק, TTL 7 ימים).
- **IndexedDB:** `mishnayon_rec` (הקלטות, store `rec`, מפתח `"chapId:m"`) · `mishnayon_tts` (קאש אודיו ענן) · `mishnayon_prefs` (store `kv`, `noShabbatNotif` — נקרא ע"י ה-SW ברקע).
- **שים לב:** עוטף ה-localStorage `S = {get,set}` מוגדר **מקומית בכל דף** — *לא* ב-utils.js. **ל-`S` בדשבורד אין `remove`** (השתמש ב-`S.set(k,null)`). בעמוד הבחירה `S` *כן* כולל `remove`.

---

## 3. 🏆 מלכודות וכללי זהב — **החלק הכי חשוב**

**קוד / מוסכמות:**
1. **`chap` מחרוזת מול מספר.** קוד מתקופת תהילים עשה `parseInt(d.id)` → NaN ("פרק NaN"/"undefined"). תבנית חוזרת — חפש אותה תמיד.
2. **`S` משתנה בין דפים.** דשבורד: `{get,set}` בלבד (אין `remove`!). בחירה: `{get,set,remove}`.
3. **`showToast` לא תמיד מוגדר.** ודא שהפונקציה קיימת לפני קריאה. **כלל: עדכן תצוגה *לפני* קריאת toast.**
4. **טקסט גלוי רבים מ-i18n.js (DICT) ומוצב כ-innerHTML.** ערוך שם, ובמפ את `?v=` של i18n.js.
5. **תבנית `<title>` חסר תג-פתיחה** — דלף טקסט גלוי ודחף את העמוד שמאלה. להיזהר ב-copy-paste.

**Sefaria / טקסטים:**
6. **ה-ref של "כלל N" בחפץ חיים מתפצל אוטומטית לסעיף א בלבד** — שורש באג "התקוע". הפתרון (`_ccFetchPieces`): לאסוף `N:1, N:2, ...` ולהציג סעיף אחר בכל יום. **לקח: ל-ref ברמת-section בדוק אם הוא מחזיר את כל הילדים או רק את הראשון.**
7. **לימודים יומיים נקיים** (אבות/רמב"ם) משתמשים בדפוס **פריט-אחד-ליום מרשימה שטוחה** (`idx % length`) — אין שם באג.
7א. 🔴 **אותה מלכודת חזרה בפירושים (תוקן 1.5.81) — הכלל החשוב ביותר מול Sefaria:**
   **`/api/texts/` (v1) עם ref חד-רמתי על טקסט בעומק 3 מצמצם אוטומטית לתת-הסעיף הראשון.**
   `Bartenura_on_Mishnah_Berakhot.1` החזיר `heRef: ברטנורא על משנה ברכות א׳:א׳` — כלומר **רק משנה א׳**.
   הקוד לקח את 10 קטעי משנה א׳ ופיזר אותם לפי אינדקס על 5 המשניות → **הוצגו 5 מתוך 30 קטעים (83% חסר), וכולם משויכים למשנה הלא-נכונה.**
   **הפתרון: `/api/v3/texts/<ref>`** — מחזיר את הפרק המלא: `versions[].text` = מערך באורך מספר המשניות, כל איבר = מערך קטעי הפירוש לאותה משנה. בוחרים `versions.find(v => v.language==='he')`.
   **תמיד לאמת:** השווה `len(text)` למספר המשניות/פסוקים בפרק. אם קיבלת 1 או מספר שלא תואם — נפלת במלכודת.
   *(קאש: מפתח שונה ל-`mishnah_comm_v2_*` כדי לבטל את הקאש הפגום אצל משתמשים קיימים.)*

**אופליין (חשוב להשקה):**
20. **הטקסט מ-Sefaria + קאש פר-פרק** (`mishnah_cache_<chapId>`, TTL 7 ימים) → לפני 1.5.76 אופליין עבד **רק** לפרקים שנפתחו לאחרונה. עכשיו `mishnah-text/<tractate>.js` הוא **fallback לכל 525 הפרקים**.
   סדר `fetchChapter`: קאש תקף → (אם `navigator.onLine===false`) קאש/מקומי מיידי → רשת → (כשל) קאש ישן → מקומי.
   - **הטעינה על-פי דרישה בלבד** (`loadTractateText`, מזריק `<script>`) — רק באופליין/כשל רשת, כדי לא להאט את הקורא באונליין. **בלי `?v=`** בכתובת, כדי שתתאים ל-precache של ה-SW (`caches.match` מדויק-URL).
   - **הטקסט המקומי נשמר לקאש (ts:0)** כדי שפירוש/שינון/הערות (שקוראים ישירות מ-`S.get(cache)`) יעבדו אופליין, ויתרענן כשיש רשת.
   - ה-SW שומר את 63 הקבצים **best-effort** (`Promise.allSettled`) — כישלון בקובץ בודד לא שובר את ההתקנה. אותו דפוס בתהילהון (`psalms-text.js`, קובץ יחיד).

**הקראה / אודיו:**
8. **Web Speech API לא ניתן לעיבוד-אודיו בדפדפן** — אי אפשר "להפוך" את הקול לבס גברי. לקול גבר אמיתי → **TTS ענן** (`api/tts.js`, `he-IL-Wavenet-D`, pitch שלילי).
9. **Autoplay בנייד:** נגן הענן חייב "שחרור" בתוך מחוות-משתמש (`_ttsUnlockAudio`).
10. **Wake Lock** משוחרר כשהטאב יורד לרקע — לבקש מחדש ב-`visibilitychange`.
11. **קול עברי ברירת מחדל נשי** (Carmit) ברוב המכשירים.

**תשתית / פריסה:**
12. **Service Worker מגיש קבצים ישנים** — בכל פריסה: **במפ `CACHE_VER`** (sw.js) + מחרוזת גרסה בדשבורד והגדרות + **`?v=`** של כל JS משותף ששונה.
13. **גרסאות:** מקדמים רק את הספרה השלישית, דו-ספרתי (1.5.xx).
14. **פריסת Vercel נתקעת לפעמים** → `git commit --allow-empty`. לאמת חי: `curl .../sw.js | grep CACHE_VER`.
15. **קידוד עברי מעורב פר-קובץ** (BOM+CRLF/LF) → **החלפה ברמת-בייטים ב-Python**, לא `sed`.
16. **Firestore דורש אימות אנונימי לקריאות.** ב-localhost נכשל → "אין נתונים" בתצוגה מקדימה, תקין.
16א. **`hasOnly` + `merge:true` ב-leaderboard:** `setDoc(...,{merge:true})` שולח ל-`request.resource.data` את **כל** מפתחות המסמך — ה-`hasOnly` חייב לכלול כל שדה. שדה חדש = להוסיף ל-`hasOnly` **וגם** לפרסם בקונסולה, אחרת כל הכתיבה נחסמת בשקט.
16ב. **שדות חדשים בלוח מובילים מתמלאים בהדרגה** → empty-state עד סנכרון. תקין, לא באג.
17. **משתני סביבה ב-Vercel:** Key = שם המשתנה, Value = הסוד. אחרי הוספה — **Redeploy**. Google TTS דורש **Billing מופעל**.

**Push / שבת:**
18. **`send-daily-push.js` שולח לכל ה-pushTokens** — לא קשור למתג המקומי. השרת חוסם **22:00–08:00** ו**שבת** (שעון ישראל).
19. **שייק:** אנדרואיד מאזין **מיד**; iOS דורש gesture להרשאת חיישן.

---

## 4. מוכנות להשקה — צ'קליסט מלא (נכון ל-1.5.80)

### א. חוסמים בקוד/ריפו
| פריט | סטטוס | הערה |
|---|---|---|
| **הסרת "BETA"** | ❌ נותר | עדיין "גרסה 1.5.xx BETA" בדשבורד+הגדרות. |
| **לוגו אמיתי** | ❌ נותר | להחליף `4.jpg` פלייסהולדר (3 מופעים: דשבורד ×2, הגדרות ×1). |
| `firestore.rules` | ⚠️ ידני | לפרסם בקונסולה בכל שינוי חוק, אחרת כתיבה נחסמת בשקט. |
| **מדריך מלא (`guide.html`)** | ⬜ חסר | קיים בתהילהון (14 פרקים, דו-לשוני). כדאי מקבילה מותאמת למשנה. |

### ב. חומרי חנות — מוכן בריפו ✅
| פריט | סטטוס |
|---|---|
| `STORE-LISTING.md` | ✅ he+en ("משנתי — שישה סדרי משנה" / "Mishnati — Daily Mishnah"). |
| `DATA-SAFETY.md` | ✅ תשובות Data Safety (כולל הערה על Google Cloud TTS כספק תשתית). |
| `terms.html` (פרטיות) | ✅ `mishnayot-alpha.vercel.app/terms.html` — כתובת חובה לקונסולה. |
| אייקון 512×512 | ✅ `icons/icon-512.png`. |
| קודי QR | ✅ `landing-mishnati/qr/` (PNG+SVG+poster). |
| דף נחיתה דו-לשוני | ✅ נכתב (מקומי) — ⚠️ צילומי המסך עדיין פלייסהולדרים. |

### ג. פעולות משתמש חיצוניות
| פעולה | סטטוס |
|---|---|
| **חשבון Google Play Developer** | ✅ **נפתח** ($25 שולם). חשבון אחד מכסה את 2 האפליקציות. |
| אימות זהות בקונסולה | ⬜ לוודא שהושלם |
| ⭐ **בדיקה סגורה: 20 בודקים × 14 יום** | ❌ **הנתיב הקריטי ללו"ז** — חובה לחשבון אישי חדש לפני פרודקשן. |
| **PWABuilder → AAB** | ❌ יוצר AAB + keystore (**לשמור!**) + `assetlinks.json` → לארח ב-`/.well-known/`. |
| **Feature graphic 1024×500** | ❌ נדרש בחנות. |
| פריסת דף הנחיתה ל-Vercel | ❌ מקומי כרגע. |
| דירוג תוכן (content rating) | ❌ שאלון בקונסולה. |

**מסקנה:** בקוד נותרו BETA + לוגו (+ מדריך רצוי). עיקר העבודה = **פעולות חיצוניות**, ובראשן להתחיל את **בדיקת 20 הבודקים (14 יום)** מוקדם ככל האפשר.

---

## 5. פיצ'רים עיקריים

### מאז 1.5.70 (הכנה להשקה)
- **אופליין מלא (1.5.76):** `mishnah-text/` — 63 קבצים פר-מסכת, טעינה על-פי דרישה + fallback ב-`fetchChapter`. ראה מלכודת #20.
- **מונה חי צף "RayBan" בקורא (1.5.75)** + `min-height:0` + מרווח נייד דינמי (`bottomnav.js`).
- **טיימר:** נעצר 10ש' אחרי סוף הפרק — **חוץ ממצב שינון** (ממשיך, ותקרת ה-7 דק' לא חלה) → מתגמל גם בשינון.
- **התקנת PWA (1.5.75):** באנר בדשבורד + כפתור קבוע בהגדרות.
- **"האפליקציות שלנו" (1.5.78–1.5.80):** קישורים לתהילהון (📖 "הבורסה של התהילים") ולעמוד הזמן (🌍). כתובת תהילהון נורמלה לשורש.
- **מייל קשר (1.5.77):** `tehilon2026@gmail.com` בכל מקומות הקשר.
- **טיקר "בורסת המשנה" (1.5.74)** — משניות חמות בקהילה (נבנה מתוך `loadDashCommunity` שבו `_db` זמין).
- **תיקוני קהילה/גרף (1.5.72–73):** ריבועי "פרקים נלמדו"/"היום" (`chaps`/`todayChaps` ב-leaderboard) · "זכויות היום" בגרף נקרא מ-`gamification.byDay`.
- **כרטיס אודות + ברכת החפץ חיים (1.5.71):** הוסר "שתפו לזיכוי הרבים"; נוסף "ודיבורי".

### קודם
7 מצבי שינון (שבב+גיליון) + הקלטה עצמית · **TTS ענן** (1.5.54, כולל כוונון קול/עומק) · סקר משתמשים (`surveys`) · שייק בכל המסכים · לוחות מובילים מרובי-מדדים · השתקת שבת + שעות שקטות · SRS (Leitner) · שיפורי היסטוריה.

---

## 6. רשימת מגירה / עתיד
- **`guide.html` למשנתי** (מקבילה לתהילהון, מותאמת: 6 סדרים, שינון, מבחן עצמי, הקראה).
- SRS פר-משנה (לא רק פר-פרק) + סשן חזרה משורשר.
- לוח חפץ חיים רשמי לפי תאריך עברי (טבלת מיפוי ידנית).
- ליטוש מצב כהה; שיתוף/הדפסת משנה; הגרלת "נחש את הספר" (להמיר ל-6 סדרים או להסיר).

---

## 7. הבדלים מתהילהון (להעברת קוד)
- **`chap` מספרי** בתהילהון מול **מחרוזת** במשנתי — התאמה בכל קוד מועבר.
- **אופליין:** תהילהון = קובץ יחיד `psalms-text.js` (~388KB, ב-APP_SHELL, נטען תמיד). משנתי = **63 קבצים פר-מסכת** (~3MB, על-פי דרישה) — כי הקורפוס גדול פי 7.
- IndexedDB: `tehillon_rec` / `tehillon_prefs` (לא `mishnayon_*`).
- פרויקט Firebase `tehilhon` · ריפו Tehilon · `tehilon.vercel.app`.
- בתהילהון **אין קול-ענן** (`api/tts.js`) — פרקי תהילים ארוכים → עלות גבוהה. **יש** לו `guide.html`, `psalms-info.js` (כרטיס מידע ל-150 פרקים), כרטיס עונתי למזמור כ"ז, ובורר "פרק קצר אקראי".
- **התחברות Google (`auth.js`) קיימת בשתיהן** — גיבוי/שחזור ענן חוצה-מכשירים, בנוסף לייצוא/ייבוא ידני.
